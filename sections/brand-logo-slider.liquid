<!-- Brand Logo Auto-Slider Section with Dual Rows (Desktop) / Single Row (Mobile) -->
<div class="brand-slider-container" style="background: {{ section.settings.background_color }};" data-auto-scroll="{{ section.settings.auto_scroll }}">
  <!-- First Row - Slides Left to Right -->
  <div class="brand-slider-wrapper">
    <div class="brand-slider-track" style="--desktop-speed: {{ section.settings.desktop_animation_speed }}s; --mobile-speed: {{ section.settings.mobile_animation_speed }}s;">
      <!-- First set of logos -->
      {% for block in section.blocks %}
        <div class="brand-logo" data-show-text-bg="{{ block.settings.show_text_background }}">
          {% if block.settings.show_image and block.settings.logo %}
            <img src="{{ block.settings.logo | img_url: '200x' }}" alt="Brand Logo {{ forloop.index }}" loading="lazy">
          {% endif %}
          {% if block.settings.show_text and block.settings.rich_text %}
            <div class="brand-text">
              {{ block.settings.rich_text }}
            </div>
          {% endif %}
        </div>
      {% endfor %}
      <!-- Second set for seamless loop -->
      {% for block in section.blocks %}
        <div class="brand-logo" data-show-text-bg="{{ block.settings.show_text_background }}">
          {% if block.settings.show_image and block.settings.logo %}
            <img src="{{ block.settings.logo | img_url: '200x' }}" alt="Brand Logo {{ forloop.index }}" loading="lazy">
          {% endif %}
          {% if block.settings.show_text and block.settings.rich_text %}
            <div class="brand-text">
              {{ block.settings.rich_text }}
            </div>
          {% endif %}
        </div>
      {% endfor %}
      <!-- Third set for extra seamless coverage -->
      {% for block in section.blocks %}
        <div class="brand-logo" data-show-text-bg="{{ block.settings.show_text_background }}">
          {% if block.settings.show_image and block.settings.logo %}
            <img src="{{ block.settings.logo | img_url: '200x' }}" alt="Brand Logo {{ forloop.index }}" loading="lazy">
          {% endif %}
          {% if block.settings.show_text and block.settings.rich_text %}
            <div class="brand-text">
              {{ block.settings.rich_text }}
            </div>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>

  <!-- Second Row - Slides Right to Left (Conditionally shown based on setting) -->
  {% if section.settings.show_second_row %}
    <div class="brand-slider-wrapper brand-slider-second-row">
      <div class="brand-slider-track brand-slider-track-reverse" style="--desktop-speed: {{ section.settings.desktop_animation_speed }}s; --mobile-speed: {{ section.settings.mobile_animation_speed }}s;">
        <!-- First set of logos -->
        {% for block in section.blocks %}
          <div class="brand-logo" data-show-text-bg="{{ block.settings.show_text_background }}">
            {% if block.settings.show_image and block.settings.logo %}
              <img src="{{ block.settings.logo | img_url: '200x' }}" alt="Brand Logo {{ forloop.index }}" loading="lazy">
            {% endif %}
            {% if block.settings.show_text and block.settings.rich_text %}
              <div class="brand-text">
                {{ block.settings.rich_text }}
              </div>
            {% endif %}
          </div>
        {% endfor %}
        <!-- Second set for seamless loop -->
        {% for block in section.blocks %}
          <div class="brand-logo" data-show-text-bg="{{ block.settings.show_text_background }}">
            {% if block.settings.show_image and block.settings.logo %}
              <img src="{{ block.settings.logo | img_url: '200x' }}" alt="Brand Logo {{ forloop.index }}" loading="lazy">
            {% endif %}
            {% if block.settings.show_text and block.settings.rich_text %}
              <div class="brand-text">
                {{ block.settings.rich_text }}
              </div>
            {% endif %}
          </div>
        {% endfor %}
        <!-- Third set for extra seamless coverage -->
        {% for block in section.blocks %}
          <div class="brand-logo" data-show-text-bg="{{ block.settings.show_text_background }}">
            {% if block.settings.show_image and block.settings.logo %}
              <img src="{{ block.settings.logo | img_url: '200x' }}" alt="Brand Logo {{ forloop.index }}" loading="lazy">
            {% endif %}
            {% if block.settings.show_text and block.settings.rich_text %}
              <div class="brand-text">
                {{ block.settings.rich_text }}
              </div>
            {% endif %}
          </div>
        {% endfor %}
      </div>
    </div>
  {% endif %}
</div>

<style>
.brand-slider-container {
  width: 100%;
  overflow: hidden;
  padding: 10px 0 10px;
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.brand-slider-wrapper {
  width: 100%;
  margin: 0 auto;
}

.brand-slider-track {
  display: flex;
  align-items: center;
  gap: 10px;
  animation: slide linear infinite;
  animation-duration: var(--desktop-speed);
  width: max-content;
}

.brand-slider-track-reverse {
  animation: slide-reverse linear infinite;
  animation-duration: var(--desktop-speed);
}

.brand-logo {
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 60px;
  min-width: 120px;
  opacity: 1;
  transition: opacity 0.3s ease;
  gap: 10px;
}

.brand-logo:hover {
  opacity: 1;
}

.brand-text a {
  text-decoration: none;
  color: #000;
}

.brand-logo img {
  max-height: 120px;
  max-width: 120px;
  width: auto;
  height: auto;
  object-fit: contain;
  filter: grayscale(0%);
  transition: filter 0.3s ease;
}

.brand-logo:hover img {
  filter: grayscale(0%);
}

.brand-text {
  text-align: center;
  font-size: 14px;
  color: #000;
  line-height: 1.4;
  max-width: 800px;
  transition: color 0.3s ease;
}

.brand-logo:hover .brand-text {
  color: #000;
}

/* Handle both image and text together */
.brand-logo:has(img):has(.brand-text) {
  min-width: 150px;
}

.brand-logo:has(img):has(.brand-text) .brand-text {
  margin-top: 8px;
}

/* Text only styling with background */
.brand-logo:not(:has(img))[data-show-text-bg="true"] .brand-text {
  font-size: 16px;
  font-weight: 500;
  padding: 5px 5px;
  border-radius: 8px;
  background: linear-gradient(135deg, rgba(187, 225, 234, 0.53), rgba(174, 174, 174, 0.9));
  transition: background 0.3s ease;
}

.brand-logo:not(:has(img))[data-show-text-bg="true"]:hover .brand-text {
  background: linear-gradient(135deg, rgba(174, 174, 174, 0.9), rgba(187, 225, 234, 0.53));
}

/* Text only styling without background */
.brand-logo:not(:has(img))[data-show-text-bg="false"] .brand-text {
  font-size: 16px;
  font-weight: 500;
  padding: 5px 5px;
  background: transparent;
  transition: color 0.3s ease;
}

.brand-logo:not(:has(img))[data-show-text-bg="false"]:hover .brand-text {
  color: #666;
}

@keyframes slide {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(-33.333%);
  }
}

@keyframes slide-reverse {
  0% {
    transform: translateX(-33.333%);
  }
  100% {
    transform: translateX(0);
  }
}

/* Auto-scroll control */
.brand-slider-container[data-auto-scroll="false"] .brand-slider-track {
  animation-play-state: paused;
}

.brand-slider-container[data-auto-scroll="true"] .brand-slider-track {
  animation-play-state: running;
}

/* Pause animation on hover (only if auto-scroll is enabled) */
.brand-slider-container[data-auto-scroll="true"]:hover .brand-slider-track {
  animation-play-state: paused;
}

/* Mobile scrollable when auto-scroll is off */
@media (max-width: 768px) {
  .brand-slider-container[data-auto-scroll="false"] .brand-slider-wrapper {
    mask-image: none;
    -webkit-mask-image: none;
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  
  .brand-slider-container[data-auto-scroll="false"] .brand-slider-wrapper::-webkit-scrollbar {
    display: none;
  }
  
  .brand-slider-container[data-auto-scroll="false"] .brand-slider-track {
    justify-content: flex-start;
    padding: 0 20px;
  }
  
  .brand-slider-container[data-auto-scroll="false"] .brand-slider-wrapper {
    padding-bottom: 10px;
  }
}

/* Mobile responsive adjustments - Hide second row on mobile */
@media (max-width: 768px) {
  .brand-slider-second-row {
    display: none;
  }
  
  .brand-slider-container {
    gap: 0;
    padding: 15px 0;
  }
  
  .brand-slider-track {
    gap: 15px;
    animation-duration: var(--mobile-speed);
  }
  
  .brand-slider-track-reverse {
    animation-duration: var(--mobile-speed);
  }
  
  .brand-logo {
    min-width: 100px;
    min-height: 50px;
  }
  
  .brand-logo img {
    max-height: 50px;
    max-width: 100px;
  }
  
  .brand-text {
    font-size: 12px;
    max-width: 100%;
  }
}

@media (max-width: 480px) {
  .brand-slider-track {
    gap: 10px;
  }
  
  .brand-logo {
    min-width: 80px;
    min-height: 40px;
  }
  
  .brand-logo img {
    max-height: 40px;
    max-width: 80px;
  }
  
  .brand-slider-container {
    padding: 10px 0;
  }
  
  .brand-text {
    font-size: 11px;
    max-width: 100%;
  }
}
</style>

{% schema %}
{
  "name": "Brand Logo Slider",
  "settings": [
    {
      "type": "checkbox",
      "id": "auto_scroll",
      "label": "Enable Auto Scroll",
      "default": true,
      "info": "Enable automatic scrolling animation"
    },
    {
      "type": "checkbox",
      "id": "show_second_row",
      "label": "Show Second Row",
      "default": true,
      "info": "Display a second row sliding in the opposite direction (hidden on mobile)"
    },
    {
      "type": "range",
      "id": "desktop_animation_speed",
      "label": "Desktop Animation Speed",
      "min": 10,
      "max": 320,
      "step": 5,
      "default": 30,
      "unit": "s"
    },
    {
      "type": "range",
      "id": "mobile_animation_speed",
      "label": "Mobile Animation Speed",
      "min": 10,
      "max": 320,
      "step": 5,
      "default": 20,
      "unit": "s"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Brand Item",
      "settings": [
        {
          "type": "checkbox",
          "id": "show_image",
          "label": "Show Image",
          "default": true
        },
        {
          "type": "image_picker",
          "id": "logo",
          "label": "Logo Image"
        },
        {
          "type": "checkbox",
          "id": "show_text",
          "label": "Show Text",
          "default": false
        },
        {
          "type": "richtext",
          "id": "rich_text",
          "label": "Brand Text",
          "default": "<p>Brand Name</p>"
        },
        {
          "type": "checkbox",
          "id": "show_text_background",
          "label": "Show Text Background",
          "default": true,
          "info": "Add gradient background to text-only items"
        }
      ]
    }
  ],
  "max_blocks": 20,
  "presets": [
    {
      "name": "Brand Logo Slider",
      "blocks": [
        {
          "type": "logo",
          "settings": {
            "show_image": true,
            "show_text": false,
            "show_text_background": true
          }
        },
        {
          "type": "logo",
          "settings": {
            "show_image": true,
            "show_text": false,
            "show_text_background": true
          }
        },
        {
          "type": "logo",
          "settings": {
            "show_image": true,
            "show_text": false,
            "show_text_background": true
          }
        }
      ]
    }
  ]
}
{% endschema %}